= form_with(model: manager, local: true) do |form|
  - if manager.errors.any?
    #error_explanation
      %h2
        = pluralize(manager.errors.count, "error")
        prohibited this manager from being saved:
      %ul
        - manager.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :name
    = form.text_field :name, id: :manager_name
  .field
    = form.label :title
    = form.text_field :title, id: :manager_title
  .actions
    = form.submit
